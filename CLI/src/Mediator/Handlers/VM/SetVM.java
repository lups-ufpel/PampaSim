package Mediator.Handlers.VM;

import Mediator.Message;
import Utils.Command;

public class SetVM implements Command {
    @Override
    public Object execute(Message message) {
        return null;
    }
}
